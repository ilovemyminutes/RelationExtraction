diff --git a/config.py b/config.py
index e98adcc..72f8e2a 100644
--- a/config.py
+++ b/config.py
@@ -35,7 +35,7 @@ class Config:
     LRFast: float = 0.005
     LR: float = 0.001
     LRSlow: float = 0.0005
-    LRSlower: float = 0.0001
+    LRSlower: float = 0.000005
 
     Seed: int = 42
     Device: str = torch.device("cuda:0" if torch.cuda.is_available() else "cpu")
diff --git a/dataset.py b/dataset.py
index 526792b..251ecbd 100644
--- a/dataset.py
+++ b/dataset.py
@@ -70,7 +70,7 @@ class REDataset(Dataset):
         raw.columns = COLUMNS
         raw = raw.drop("id", axis=1)
         raw["label"] = raw["label"].apply(lambda x: enc.transform(x))
-        # raw = raw[raw['label'] != 0].reset_index(drop=True)
+        raw = raw[raw['label'] != 0].reset_index(drop=True)
         print(f"preprocessing for '{preprocess_type}'...", end="\t")
         data = preprocess_text(raw, method=preprocess_type)
         print("done!")
diff --git a/wandb/latest-run b/wandb/latest-run
index 178711f..96a422f 120000
--- a/wandb/latest-run
+++ b/wandb/latest-run
@@ -1 +1 @@
-run-20210418_102228-1jagit0t
\ No newline at end of file
+run-20210418_102922-1luc0937
\ No newline at end of file
